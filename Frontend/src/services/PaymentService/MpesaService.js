const axios = require('axios');

const payment_credentials = async (req, res) => {
    try {
        const response = await axios.post();
    } catch (error) {
        console.error("Error in posting payment details: ", error.message);
    }
};